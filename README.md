# Response Guard for SillyTavern

一个用于 SillyTavern 的轻量前端扩展：  
当 AI 忘记在回复末尾附带你要求的“小总结”“剧情推荐选项”等结构时，你可以点击按钮，让它只检查并补齐缺失部分，而不是重 roll 整条回复。

补齐结果会直接追加到**最新那条 AI 回复本身**，不会额外新建一条消息。

当前版本：`0.24.2`

## 功能

- 可自由编辑“格式规范 / 检查规则”
- 支持多个补全方案，并可绑定到不同角色卡自动切换
- 一键“仅检查”
- 一键“补齐到当前回复”
- 新增魔法棒菜单直达按钮：可在聊天输入框旁的扩展菜单里直接“补齐格式”或打开“局部修改”
- 暴露 `window.ResponseGuard` 简易接口，便于酒馆助手 / 其他脚本调用
- 新增“归纳格式修复指导”模块：可用一次正确回复 + 模块要求，让 AI 归纳出可独立使用、不引用原始资料的补全规则
- 新增“局部修改最新回复”模块：可按要求修改最新回复中间的某些内容，先生成完整回复预览，再确认覆盖最新回复当前 swipe
- 格式检查/补齐、格式指导归纳、局部修改预览生成时，右下角发送键会切换为旋转中的取消态
- 功能区域默认折叠，减少面板高度，方便直接点击“补齐到当前回复”
- 两种调用模式：
  - 使用 SillyTavern 当前已连接 API
  - 使用你自定义的 OpenAI-compatible API
- 自定义 API 支持点击“获取模型”自动请求模型列表，再从下拉框选择
- 只把“最新回复 + 规则”送去检查，不会为了这个动作重发整段聊天

## 安装

### 通过仓库链接安装（推荐）

在 SillyTavern 中打开：

```text
Extensions → Install extension
```

粘贴仓库地址：

```text
https://github.com/baiming12/sillytavern-response-guard
```

安装完成后重启或刷新 SillyTavern，即可在扩展设置中看到 **Response Guard**。

> 仓库根目录必须直接包含 `manifest.json`、`index.js`、`settings.html` 等文件。  
> 如果上传时又多包了一层文件夹，SillyTavern 可能会把扩展显示成 `undefined`。

### 手动本地安装

如果你只想自己本地使用，把整个 `response-guard` 文件夹放到：

```text
SillyTavern/data/<你的用户>/extensions/response-guard
```

然后重启 SillyTavern，在扩展设置里就能看到 **Response Guard**。

> 如果你想“给所有用户安装”或做本地开发，也可以放到 `public/scripts/extensions/third-party/response-guard`。  
> 注意：`third-party/...` 也是扩展在前端被挂载后的访问路径，并不代表当前用户的磁盘目录下一定要手动创建 `third-party` 文件夹。

如果你想通过 SillyTavern 的 **Install extension** 功能一键安装，直接使用本仓库地址即可。

## 使用

1. 在“格式规范 / 检查规则”里写下你当前想让模型遵守的附加结构。
2. 选择调用方式：
   - **使用酒馆当前 API**：最省事，直接复用你当前的连接；
   - **自定义 OpenAI-compatible API**：填写 API 地址、模型名、Key。
3. 当最新回复漏项时：
   - 点 **仅检查**：只告诉你缺了什么；
   - 点 **补齐到当前回复**：自动把缺失段落直接追加到最新 AI 回复末尾（同层修改，不新建消息）。

## 默认规则示例

```text
每条回复末尾必须包含：
1. 【本回合摘要】1-3 句，概括最新剧情推进
2. 【下一步建议】仅当剧情出现明显分岔时给出 2-4 个选项；若当前不适用，则明确写出“本回合暂无需要选择的分岔”

检查时只判断“最新回复”是否满足这些要求，不要要求重写正文。
```

## 注意事项

- “当前 API”模式最稳，因为它走的是 SillyTavern 自己的生成接口。
- “自定义 API”模式目前按 OpenAI-compatible `chat/completions` 方式直连；如果你的服务端不允许浏览器跨域请求，可能会被 CORS 拦住。
- “获取模型”按钮会按 OpenAI-compatible 约定请求 `/models`；如果你的服务端没有实现这个接口，仍可继续手填模型名。
- 自定义 API Key 会随着扩展设置一起保存；如果你不想额外保存一份密钥，优先用“当前 API”模式。
- 当前版本保持手动触发，不会在每次回复后自动检查；避免误判生成时机，也避免无意中多花 token。
- 当前补齐方式更像“可配置的继续补尾巴”，但为了省 token，它不是直接复用整段聊天上下文继续写，而是只针对最新回复做补齐判断。
- “局部修改最新回复”会把最新回复整体发给模型，并要求模型只改你指定的位置；应用前会显示完整预览，确认后才覆盖当前最新回复。
- 如果归纳结果很长，结果框会自动回到开头显示；自定义 API 模式下会提高归纳输出上限，并在模型明确返回长度截断时给出提示。

## 更新记录

### 0.24.2

- 局部修复预设默认只显示名称，不再展开完整要求。
- 点击“编辑”会打开较大的弹窗编辑器，方便修改长预设内容。

### 0.24.1

- 优化局部修复预设显示：勾选列表和预设管理列表中的长要求会限制高度并在内部滚动，避免把底部按钮顶得太远。

### 0.24

- 移除“自动格式修复”开关和自动事件监听，恢复为手动触发，避免流式输出或开场白场景误触发。
- 保留后台生成忙碌态：格式检查/补齐、归纳格式修复指导、局部修改预览生成时，右下角发送键仍会切换为旋转中的取消态。
- 加固“归纳格式修复指导”提示词：生成的规则更强调可追加、不可重写正文、不可复读样例、避免因小差异重复补模块。

### 0.23.3

- 自动格式修复改为等待主生成结束后再启动；`MESSAGE_RECEIVED` / 渲染事件在流式输出期间只记录状态，不再直接触发检查。
- 增加最新回复稳定检测：生成结束后内容连续稳定约 2.2 秒才会开始格式修复，避免主 API 还在输出剧情时被打断。
- 如果酒馆发送按钮仍显示停止/取消/生成中状态，自动修复会继续等待。

### 0.23.2

- 自动格式修复只在真实生成窗口内触发：需要先收到 `GENERATION_STARTED`，再由生成结束、AI 消息写入或渲染事件排队检查。
- 切换角色、加载/新建聊天时会清空待处理自动修复，避免角色卡预设开场白被自动追加格式模块。

### 0.23.1

- 自动格式修复触发改为多事件兜底：除生成结束外，也会在 AI 消息写入或渲染后排队检查，提升第一条回复和不同 SillyTavern 版本下的可靠性。
- 发送按钮识别更稳：优先查找输入框附近的纸飞机/发送按钮；如果主题或移动端布局找不到原按钮，会显示一个临时旋转取消按钮。

### 0.23

- 新增“自动格式修复”开关：开启后在正文生成结束时自动检查最新回复，并在缺少附属模块时追加到当前回复末尾。
- 统一后台生成状态：格式检查/补齐、归纳格式修复指导、设置页局部修改、魔法棒局部修改都会把右下角发送键切换为旋转中的取消态；点击可取消本次 Response Guard 生成并阻止结果写入。

### 0.22

- 将“方案与角色绑定”“格式规范 / 检查规则”“归纳格式修复指导”“API 调用设置”改为默认折叠区域。
- 修正浅色主题下折叠卡片“灰底黑字”观感问题，改为跟随 SillyTavern 主题色的半透明卡片。
- 优化底部“仅检查 / 补齐到当前回复”主操作区按钮宽度，避免按钮文字竖排或挤在一起。
- 修正归纳结果框可能停留在文本末尾、看起来像只剩后半段的问题；自定义 API 归纳请求提高输出上限，并在触发长度限制时提示。
- 新增“局部修改最新回复”折叠模块，支持输入修改要求、生成完整修改预览、复制结果，并在确认后覆盖最新回复当前 swipe。
- 新增聊天界面魔法棒菜单入口：“Response Guard：补齐格式”和“Response Guard：局部修改”。
- 修复手机端从魔法棒打开“局部修改”时，弹窗可能被挤到页面顶部且高度异常的问题。
- 新增 `window.ResponseGuard` 脚本接口：`checkOnly()`、`checkAndFix()`、`openRewriteDialog()`、`rewriteLatest(instruction, options)`。


### 0.3.0

- 新增“归纳格式修复指导”模块：提供“正确回复样例”和“模块要求”两个输入框，调用 AI 归纳出可直接用于格式规范的修复指导。
- 归纳结果可在面板内编辑、复制，或一键追加到当前方案的格式规范。
- 归纳样例、模块要求和归纳结果会随当前方案保存，切换方案时自动切换。

### 0.2.0

- 新增“多方案”管理：可以新建、复制、删除不同格式补全方案。
- 新增“绑定当前角色卡”：当前角色卡绑定某个方案后，切换到该角色卡会自动使用对应方案。
- 每个方案独立保存规则、调用方式、温度、自定义 API 地址、模型名和 API Key。
- 优化按钮样式，补齐按钮改成更明显的主按钮。

### 0.1.2

- 补齐后触发与原生编辑流程一致的消息更新事件，便于前端美化逻辑立即重新生效
- 将“已知取舍”调整为“注意事项”，移除仅对维护者有用的说明

### 0.1.1

- 修复通过 GitHub 安装后，因仓库目录名不同导致设置面板模板路径失效的问题
- README 增加 GitHub 上传目录结构提示

### 0.22 补充：局部修改要求预设

本版基于 `0.22-mobile-dialog` 可用版本添加，不依赖新的 settings.html 结构；预设管理区会由脚本动态插入到“局部修改最新回复”区域。

- 全局预设：所有角色卡都能看到和勾选。
- 本角色卡预设：只在当前角色卡显示和勾选。
- 设置面板和魔法棒“局部修改”弹窗都会显示可勾选预设。
